<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_height="wrap_content"
    android:layout_width="wrap_content"
    android:layout_margin="@dimen/marginSmall"
    >

    <ImageView
        android:id="@+id/projectItemImageView"
        android:layout_width="@dimen/projectitem_ll_width"
        android:layout_height="@dimen/projectitem_ll_width"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"

        >

    </ImageView>

    <TextView
        android:id="@+id/projectItemTextView"
        android:layout_width="@dimen/projectitem_ll_width"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/textSizeMedium"
        android:textColor="@color/kwc_gray_background"
        android:background="@color/kwc_gray_item"
        />
</LinearLayout>